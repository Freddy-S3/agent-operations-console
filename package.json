{
  "name": "agent-operations-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/domain.js && node --check src/adapters.js && node --check src/orchestrator.js && node --check src/server.js && node --check public/app.js",
    "build": "npm run check"
  }
}
